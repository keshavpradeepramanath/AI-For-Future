def call_gpt35(query):
    return f"[GPT-3.5] {query}"

def call_gpt4o(query):
    return f"[GPT-4o] {query}"

def call_gemini(query):
    return f"[Gemini] {query}"

def call_claude(query):
    return f"[Claude] {query}"
