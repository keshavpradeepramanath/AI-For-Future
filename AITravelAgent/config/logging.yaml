version: 1

formatters:
  standard:
    format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

handlers:
  file:
    class: logging.FileHandler
    filename: logs/app.log
    formatter: standard
    level: INFO

root:
  handlers: [file]
  level: INFO
